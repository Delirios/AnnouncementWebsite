@model AnnouncementListViewModel

<main role="main">
    <div class="album py-5 bg-light">
        <div class="container">
            <h1 align="center">Your announcements</h1>
            <div class="row">
                @foreach (var announcement in Model.Announcements)
                {
                    <partial name="_AnnouncementCard" model="announcement" />
                }
            </div>
        </div>
    </div>
</main>